# CPU subset -- verified locally.
#   conda create -n llmcite python=3.11 -y && conda activate llmcite
#   pip install -r requirements.txt
#   python -c "import nltk; nltk.download('stopwords'); nltk.download('punkt'); nltk.download('punkt_tab')"
pandas<2.3
numpy<2
nltk
scikit-learn
rank_bm25
langchain-core      # required to unpickle the stored Document objects

# GPU additions -- needed for extract_keywords.py, measure_efficiency.py and
# ALCE's eval.py. See SERVER.md for the VRAM/disk requirements.
# torch
# transformers
# sentence-transformers
# accelerate
